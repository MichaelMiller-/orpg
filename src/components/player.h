#pragma once

namespace orpg
{
   struct player
   {
      // bool isAttacking = true;
      // bool isHit = true;
      // Vector2 attack_direction;
      // float attack_rate = 0.5;
      // float dt = 0;
      // int score = 0;
      // std::string name = "abc";
   };
}