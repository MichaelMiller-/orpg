#pragma once

namespace orpg
{
   struct layer1 {}; // ground, floor
   struct layer2 {}; // entities
   struct layer3 {}; // overlays (gate, tunnel, etc)
   struct layer4 {}; //
   struct layer5 {}; // weather (clouds, rain, fog, etc)
}